#!/usr/bin/env node
var argv = require('optimist')
    .string('left', 'top')
    .argv
;
console.dir([ argv.left, argv.top ]);

/* Turns off numeric coercion:
    ./node string.js -left 000123 -top 9876
    [ '000123', '9876' ]
*/
