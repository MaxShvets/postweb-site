var optimist = require('../');
var test = require('tap').test;

test('whitespace should be whitespace' , function (t) {
    t.plan(1);
    var x = optimist.parse([ '-left', '\t' ]).left;
    t.equal(x, '\t');
});
